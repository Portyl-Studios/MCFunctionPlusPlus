import { test } from '@playwright/test'

test.describe.skip('desktop broad workflows', () => {
  test('launch app, open workspace, and save file flow', async () => {
    test.fail(true, 'Implementation scaffold: enable after Electron launch harness is added')
  })
})
